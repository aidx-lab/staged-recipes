
[project]
name = "racket"
version = "0.1.0"
channels = ["conda-forge"]
preview = ["pixi-build"]
platforms = ["linux-64", "osx-arm64"]

[package.build]
backend = { name = "pixi-build-rattler-build" , version = "*"}
